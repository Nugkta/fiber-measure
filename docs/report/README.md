# report/

Once my folder has changes, please update me.

One report per study whose main experiment has run, named `<NN>_<slug>.md` with the same stem as the
study's labbook file in `docs/labbooks/`. Each report: intro, method, results,
discussion.

## Files

- `01_esf_edge_consistency.md` — current — study 01 (erf edge refinement)
  M5 A/B validation on the full MasP2 set: method, tuning path, per-group
  results, acceptance verdict (coverage PASS, replicate-consistency FAIL) and
  recommended next steps. Written at the end of the main experiment; the study
  itself is still `active` pending a decision on a better replicate set.
- `01_esf_ab_summary.png` — current — three-panel evidence figure for that
  report: per-group primary metric off vs on, refinement coverage before/after
  the gate tuning, and the focus dependence of the measured diameter.
